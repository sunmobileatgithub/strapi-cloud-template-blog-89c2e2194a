'use strict';

/**
 * available-rate-plan service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::available-rate-plan.available-rate-plan');
